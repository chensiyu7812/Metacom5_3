> 2026-09-11 发布说明：本报告为 2026-09-08 的 V2 交付核验快照。正文的“尚未提交或推送”描述当日状态；本次发布将 V2 实现、契约和审计材料纳入 GitHub。问卷、评分、截图和运行缓存仍按仓库规则保存在本地。
>
> 当前状态及未完成项见[网页版审阅入口](../../PM_PAPER1_WEB_REVIEW_20260911_ZH.md)。本目录面向研究负责人和方法审阅者，不是发给主评的评分材料。

# Metacom5_3 人评 V2 交付核验（2026-09-08）

结论：新版空白卷可开始独立评审。此结论针对人评交付与对应 teacher reference，不代表正式研究链路的所有历史问题已解决。

## 授权及变更

研究者在本会话授权生成新版。修订 DG 参考只含 related sessions、生成却可利用完整过去历史的证据不对称；在原摘录之外提供相同用户严格过去的完整原始会话，附日期索引和字面搜索。人机 reference 内容相同，双方回复共享同一证据。

保持 80 个基础 pair、16 个反序、每人 96 题；已有 142 条生成回复、题目、A/B 映射及各自顺序均不变。两个原 V1 文件与所有原 tracked authority 均未改动。未产生任何真实人类评分，没有新生成、Gemini 调用、API 花费、formal outcome 或 PM training。

两位主评原始评分先封存，再按预定共识流程处理分歧、任一 uncertain 或反序不稳定；无法形成有依据的共识则 uncertain。主评一致性基于原始数据，共识另计且先于 Gemini verdict 封存。没有新设经验 PASS 门槛。

## 核验结果

| 核验 | 结果 |
|---|---|
| 现行 Paper-1 非 GPU suite | 817 项全部通过；11 项 GPU 测试按现行 CI 规则排除 |
| Integration validator | 32/32，通过；四把 outcome locks 全部 CLOSED |
| 浏览器实际操作检查 | 33/33，通过；Chrome 136.0.7103.92 |
| 分发包与原始产物独立核验 | 28/28，通过 |
| 离线重构逐字节比对 | 15 个产物全部一致，`61_materialize_human_reference_v2.py --check` |
| 全部源码/脚本/测试 compileall 与表单 JS 语法 | 通过 |
| 接收 CLI | 原始空卷允许作为 0/96 进度；作为完整提交时正确拒绝，退出码 1 |
| git diff --check | 通过 |

浏览器逐项核对两位评审共 192 次呈现的原题、两个回复与 reference；DG 完整历史的角色、日期和原文逐字一致。63 个 DG candidate instances 的来源均被九个完整过去历史覆盖。

真实问卷只做只读展示核验，无勾选、无理由填写。保存/恢复/导出等交互全部使用 `/tmp/paper1-human-v2-ui-fixture/` 的两题人工 UI 测试数据，包含清晰的 UI TEST ONLY 标记，不进入任何研究答卷、标签或分发包。

浏览器检查覆盖：无预选；历史搜索/清空/日期跳转；桌面和手机布局；脚本样式文字安全展示；分次保存和刷新恢复；部分及完整导出；本人进度导入；篡改、另一评审文件、重复 JSON 字段拒绝；本机存储不可用时提示且导出仍可用；JavaScript 错误为 0，外部网络请求为 0。

每份 ZIP 只含本人的 HTML、空白 JSON 和说明，ZIP 成员字节与独立文件一致，所有文件及哈希匹配 manifest。回答文本、问卷和 coordinator 请求均在 Git 忽略的 outputs 下。当前改动尚未提交或推送；本轮未运行远端 CI。

## 交付与后续

产物：`/opt/tokkio-data0/tokkio_projects/Metacom5_3/project/outputs/paper1_pairwise_teacher/human_sheets_v2/`。A、B 各发自己的 ZIP，打开其中 HTML。允许分次填写，每次导出进度备份，最终交回 `_rated.json`。

仓库说明：`project/docs/PM_PAPER1_HUMAN_REFERENCE_V2_HANDOFF_20260908_ZH.md`。当前 authority：qualification plan V3、2026-09-08 reference/instrument/design/identity/manifest V2。

新卷用于校准自动裁判的测量参照，不直接作为 PM 训练信号，不构成自然轮次克制性或最终 top-k 结论。完成主评及分歧处理后，再单独授权 Gemini qualification；随后进行真正的 amount/cap calibration。2026-09-07 审计中的 Summary equivalent 编码、teacher 目标与后续验证隔离、正式 DG 预算重算等仍在相应正式阶段前处理。

机器证据：`browser_qa.json`、`package_qa.json`、`integration_validation.json`、`test_collection.txt`。浏览器复核脚本：`ui_check.py`；截图：`rater_a_desktop.png`、`rater_b_desktop.png`、`rater_a_mobile.png`、`rater_b_mobile.png`。
