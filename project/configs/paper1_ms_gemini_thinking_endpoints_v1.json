{
  "protocol": "pm-v1.5-paper1-ms-gemini-thinking-review-endpoints-v1",
  "candidates": {
    "google_gemini_2_5_flash_thinking": {
      "api_key_env": "GEMINI_API_KEY",
      "base_url": "https://generativelanguage.googleapis.com/v1beta",
      "family": "google_gemini_2_5_flash_thinking",
      "gemini_thinking_budget": 2048,
      "input_usd_per_million_tokens": 0.3,
      "model": "gemini-2.5-flash",
      "output_usd_per_million_tokens": 2.5,
      "supports_strict_json_schema": true,
      "transport": "gemini_generate_content"
    }
  },
  "request_contract": {
    "client_internal_retries": 1,
    "maximum_physical_attempts_per_logical_call": 2,
    "max_output_tokens": 4096,
    "seed": 20260812,
    "temperature": 0.0
  },
  "design_note": "Isolated endpoint config for the Gemini-thinking-enabled probe only (paper1_ms_source_annotated_review_endpoints_v1.json is a hash-bound artifact referenced by multiple frozen phase contracts and must not be mutated). Same model and per-token rates as the frozen config's google_gemini_2_5_flash candidate; only gemini_thinking_budget (0 -> 2048) and max_output_tokens (1200 -> 4096, to leave room for both thinking and the structured answer) differ."
}
