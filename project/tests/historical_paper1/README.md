# Historical Paper-1 tests

These tests preserve superseded V3, 2026-08 prequalification/resource-amount,
and semantic-memory v7/v8/v9 DEV provenance. They are intentionally outside
the routine `tests/test_paper1_*.py` CI selection and do not define current
research progression gates.

Run an individual file explicitly only when auditing its archived artifact.
Current Paper-1 integrity is covered by the root-level `test_paper1_*.py`
suite, including `test_paper1_current_artifact_integrity.py`.
